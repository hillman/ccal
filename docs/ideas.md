## Live Notes
 - external markdown docs sort of symlinked into the repo - the system that adds them copies them into the repo, and whenever they're opened, they get resynced.  machines that pull these paths down and dont have the relevant file, don't sync in, they just use what's there.  

## Bookmarks - assign number keys to certain notes


